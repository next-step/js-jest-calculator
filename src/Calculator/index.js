class Calculator {
  sum(a, b) {
    return Math.floor(a + b);
  }

  subtract(a, b) {
    return Math.floor(a - b);
  }

  multiply(a, b) {
    return Math.floor(a * b);
  }
}

export default Calculator;
