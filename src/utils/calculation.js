export const add = (firstNumber, secondNumber) => firstNumber + secondNumber;
export const divide = (firstNumber, secondNumber) => firstNumber / secondNumber;
export const multiply = (firstNumber, secondNumber) => firstNumber * secondNumber;
export const subtract = (firstNumber, secondNumber) => firstNumber - secondNumber;
